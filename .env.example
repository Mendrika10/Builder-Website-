# ============================================================
#  Site.mg — Variables d'environnement (racine)
#  Copier vers .env et adapter. JAMAIS de valeurs réelles ici (§22).
# ============================================================

# ---- Docker compose (FOND-010) : PostgreSQL + Redis ----
POSTGRES_USER=sitemg
POSTGRES_PASSWORD=sitemg_dev
POSTGRES_DB=sitemg

# ---- v1 (Next.js fullstack, référence fonctionnelle) ----
DATABASE_URL="mysql://root:VOTRE_MOT_DE_PASSE@localhost:3306/builder_website"
AUTH_SECRET="openssl rand -hex 32"
AUTH_URL="http://localhost:3000"

STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

UPLOAD_PROVIDER="local"   # local | s3 | cloudinary
NEXT_PUBLIC_UPLOAD_URL="/uploads"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_NAME="Site.mg"

# ---- Trello (suivi sprint — voir docs/team/trello.md) ----
TRELLO_API_KEY=""
TRELLO_TOKEN=""
