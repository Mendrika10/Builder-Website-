# ============================================================
#  @site-mg/web — Variables d'environnement
#  Copier vers .env.local et adapter. JAMAIS de valeurs réelles ici (§22).
# ============================================================

# URL de l'API NestJS (apps/api)
NEXT_PUBLIC_API_URL="http://localhost:3201"

# Port du serveur Next (défaut 3200, sinon passer -p au script dev)
PORT=3200
