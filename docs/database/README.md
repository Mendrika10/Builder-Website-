# Database — Schéma & migrations

Cette section documentera le schéma PostgreSQL et les migrations à partir du Sprint 01 (tâche FOND-020 : `user`, `email_verification`, `plan`, `subscription`, `site`).

Le modèle fonctionnel de référence est décrit dans [architecture/database.md](../architecture/database.md).

Rappel ([règle Database](../../AGENTS.md#21-règle-database)) : toute modification du schéma passe par une migration versionnée, testée, reviewée avant merge.
